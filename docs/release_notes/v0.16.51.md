# Trinity v0.16.51 – Desktop-Launcher lädt neue Komponenten zuverlässig

## Behoben

- Der direkte Start über `trinity_launcher.py` ergänzt den lokalen `core`-Pfad,
  bevor Canvas- und Reset-Komponenten geladen werden.
- Dadurch funktionieren dieselben neuen Bausteine sowohl über die CLI als auch
  über Trinity.app beziehungsweise den Windows-Desktopstarter.

## Prüfung

- neuer Launcher-Importtest in einem frischen Python-Prozess
- kombinierter Launcher-, Canvas- und Health-Test
- vollständige Test-Suite
- GitHub-Smoke-Tests unter macOS und Windows
