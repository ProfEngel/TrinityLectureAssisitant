# Trinity v0.16.50 – Canvas nur lokal erreichbar

## Behoben

- Die eingebundene Canvas-Komponente wurde auf 0.2.1 aktualisiert.
- Canvas berücksichtigt beim Produktionsstart die von Trinity vorgegebene
  Host-Adresse und bindet standardmäßig ausschließlich an `127.0.0.1`.
- Der interne Canvas-Dienst ist damit nicht unbeabsichtigt im lokalen Netzwerk
  erreichbar.

## Prüfung

- Canvas-Typecheck und Produktions-Build
- erfolgreicher Health-Endpunkt
- Socket-Prüfung bestätigt `127.0.0.1:8787` statt `*:8787`
- vollständige Trinity-Test-Suite unter macOS
- plattformübergreifende GitHub-Smoke-Tests für macOS und Windows
