# v0.16.25 - Pi duplicate payload hotfix

## Highlights

- Pi results now return as a normal direct Trinity answer without an additional HTML payload card when the card would only repeat the same text.
- This removes the duplicate display where the same Pi answer appeared once in chat and once again in a blue `Pi` result box.
- Media, calculation, simulation and other real payload-producing agents are unchanged.

## Verification

- `python3 -m py_compile agents/pi_agent/script.py`
- `python3 -m pytest -q tests/test_pi_agent.py tests/test_brain_direct_answer.py`
- `python3 -m pytest -q`
